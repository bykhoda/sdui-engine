{
  "version": "0.1",
  "screen": {
    "id": "card_roadmap",
    "title": "roadmap",
    "content": {
      "type": "vstack",
      "spacing": "$token.spacing.md",
      "alignment": "leading",
      "modifiers": { "padding": "$token.spacing.lg" },
      "children": [
        { "type": "text", "value": "roadmap — numbered steps, done & disclosable detail", "style": "$token.typography.caption", "color": "$token.color.textSecondary" },
        {
          "type": "roadmap",
          "accent": "$token.color.primary",
          "modifiers": { "size": { "width": { "mode": "fill" } } },
          "steps": [
            { "title": "Design the contract", "detail": "One JSON schema shared by every renderer.", "done": true },
            { "title": "Ship iOS + Android", "detail": "Both read the same payloads.", "done": true },
            { "title": "Add the snapshot harness", "detail": "See each screen on every platform, side by side." },
            { "title": "Wire the CI gate", "detail": "A drift on any platform turns the PR red." }
          ]
        }
      ]
    }
  }
}
